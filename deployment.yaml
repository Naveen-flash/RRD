apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  labels:
    app: auth-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: IMAGE_URI  # This will be replaced in your Jenkins pipeline
          ports:
            - containerPort: 3001
          env:
            - name: FLASK_ENV
              value: production
            - name: JWT_SECRET
              value: your-super-secret-jwt-key
